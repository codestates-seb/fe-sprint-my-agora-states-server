import { useCallback } from "react";

const UseDeleteRequest = (url, body, method) => {
    // const callAPI = useCallback( async () => {
    //     const datalist = await
    // });
};

export {UseDeleteRequest};